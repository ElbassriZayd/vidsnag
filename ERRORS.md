# Errors

Scan at session start. Add every failure here immediately.

| Date | Area | What failed | Root cause | Fix |
|------|------|-------------|------------|-----|
| 2026-05-29 | Filesystem | File writes failed / corrupted (README.md → 0 bytes) | C: drive 100% full (<50 MB free); ENOSPC truncates open files to 0 bytes | Free disk space; set pagefile AutomaticManagedPagefile=true; reboot; rewrite corrupted files. See global memory feedback_disk_full_silently_kills_pagefile |
| 2026-05-30 | App icon | Window/taskbar showed the python snake, not VidSnag | WebView2 backend ignores webview.start(icon=) | Set icon via pywin32 WM_SETICON after window load + ctypes AppUserModelID for taskbar |
| 2026-05-30 | Logo cutout | Background-removal left grey haze / washed-out V | Brightness + single-corner color-distance masks fail on a navy GRADIENT bg | Use saturation matte (max-min channel) — logo is saturated, bg is dull. scripts/cut_logo.py |
| 2026-05-30 | Website CSS | Page rendered fully unstyled on file:// | Absolute asset paths (/styles.css) resolve to drive root on file:// | Use relative paths (styles.css). Works on file:// AND Vercel |
| 2026-05-30 | Website mobile | Hero/sub appeared clipped on narrow viewport | Scrolling marquee + ticker (width:max-content flex) forced page width; also Edge headless reported 459px viewport at --window-size=390 (artifact) | overflow:hidden + min-width:0 on wraps, html/body max-width:100vw. Verify with computed clientWidth==scrollWidth, not the screenshot |
| 2026-05-30 | MCP | 21st.dev magic "Failed to connect" | (a) corrupted npx cache: is-inside-container missing from is-wsl; (b) MCP only attaches at Claude Code startup | rm -rf ~/AppData/Local/npm-cache/_npx (boots clean); RESTART Claude Code to connect |
| 2026-05-30 | Distribution | Browser/SmartScreen "VidSnag.exe isn't commonly downloaded" scared the user (thought = virus) | NOT a virus: it's SmartScreen REPUTATION for a new UNSIGNED exe (Windows still offers Open/Save; a real threat is blocked). No free instant fix | Reassure (it offers Open/Save = Defender passed). Mitigate free: version metadata + open-source + SHA-256 + VirusTotal badge + FAQ. Real fix = code-signing (SignPath free OSS / EV paid) |
| 2026-05-30 | Distribution | VirusTotal showed 1/69 "malicious" | Bkav Pro false-positive — it flags virtually ALL PyInstaller onefile exes; not a real detection | Ignore single-Bkav flags on PyInstaller builds. Microsoft Defender + all majors = Undetected. Embedding version metadata reduces FP surface |
| 2026-05-30 | Distribution | Suspected pywin32 (FindWindow/SendMessage icon code in desktop.py) made the app "look untrusted" | TESTED empirically: stripped no-win32+no-ffmpeg build scored 3/71 incl Microsoft `Program:Win32/Wacapew.C!ml` + SecureAge — WORSE than the shipped 1/69. win32 EXONERATED. The PyInstaller packer is the trigger; the larger content-rich shipped build evades Microsoft's ML flag that fires on small generic stubs | KEEP win32 (it's just the taskbar icon) + KEEP the shipped 189MB build (Microsoft-clean). "Win32" in the detection NAME = Microsoft's "Windows program" malware taxonomy, NOT our pywin32 code. Real fix stays = code-signing |
| 2026-05-30 | Playwright | Bundled chromium not installed (`chrome-headless-shell` missing) | Playwright in manzili_2 node_modules has no browser downloaded | Use `chromium.launch({channel:'msedge'})` — drives system Edge. For bot-protected sites (VirusTotal) use `headless:false` |
